# the-codecrafters

 After successfully writing your program and everything is running as expected,to run the program you have to use go run(filename.go) and the output should print the options for entering input1 after which the options for input2 would pop.

# NOTE: 
 It's not compulsory for option input2 to pop after inputing1 it's all depends on the arrangements of your code(program).

 After inserting all inputs1/2 it should request for the operator to use whether (+, -, *, /).

 Then it should sum the value of input1 & input2 together if the operator used is the addition operator.

 # NOTE:
 The goal of the program is to continue and not crash no matter the input been entered it should always run except the user decides to quit the program.

 All edge cases should be tested to check the correctness of the program and if all the conditions are met then you have successfully implemented a functioning **CLI CALCULATOR**.